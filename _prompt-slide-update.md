# 강의자료 생성 프롬프트

**사용법**: 같은 종류의 기존 qmd 파일 하나를 함께 첨부하고 아래 프롬프트를 붙여넣습니다.
형식, YAML, 색상, 파일 경로 같은 것은 첨부 파일이 알려 주므로 프롬프트에 적지 않습니다.
`[대괄호]`만 채우세요.

---

## 공통 (세 프롬프트 앞에 붙이기)

```
- 첨부 파일과 같은 형식, 구조, 문체로 작성
- 용어: "자료" 대신 "데이터", "처치군/대조군" 대신 "처치집단/통제집단"
- 업데이트하려는 슬라이드 초안이 이미 작성되어 있는 경우 텍스트와 내용은 최대한 유지
```

---

## 1. 강의노트

```
[공통]

첨부한 [파일명]과 같은 형식으로 「[주제명]」 강의노트를 만들어 주세요.

구조
- 도입: 헤딩 없이 산문 3~5문단. 구체적 상황이나 실제 연구 사례에서 출발해
  기존 방법의 한계로 마무리. 사례는 저자와 연도 표기
- 본문: ## 수준 절 3~5개, 절 사이는 `***` 와 `<br>` 로 구분

서술
- 개념 → 수식 → 코드 순서. 수식은 말로 먼저 설명하고 기호를 불릿으로 나열
- 코드는 `#| eval: false`, 인수마다 역할을 주석으로
- 콜아웃: note(보충), important(흔한 오해), warning(실행 시간·버전).
  심화는 `collapse="true"`

반드시 포함
- 이 방법이 의존하는 가정과 그것이 깨졌을 때 무엇이 잘못되는지
- 앞선 강의 방법과의 대응 표
- 결과 해석 시 흔한 오류
```

---

## 2. 강의 슬라이드

```
[공통]

첨부한 [파일명]과 같은 형식·문체로 「[주제명]」 슬라이드를 만들어 주세요.
분량은 [N]분, 대단원은 [A], [B], [C]입니다.

문체
- 불릿과 문장을 명사형으로 종결. "~합니다" 체를 쓰지 않음
  (예: "손실을 가장 작게 만드는 파라미터를 탐색", "신경망도 예외가 아님")
- 볼드는 거의 쓰지 않음. 하지만 슬라이드 초안이 볼드를 사용하는 경우 그대로 유지.
- 슬라이드 한 장에 논점 하나, 불릿 5개 이하
- 여백은 `<br>` 로 조절. `. . .` 과 `::: {.incremental}` 은 쓰지 않음
- 학습 목표 슬라이드와 마지막 정리 슬라이드는 [넣음 / 넣지 않음]

코드
- 그림 생성 청크는 `#| echo: false` + fig 옵션. 오래 걸리면 `#| cache: true`
- 보여주는 코드는 `#| eval: false` 와 `#| echo: true`
- 코드 안에 `# 1단계. 구조 정의` 처럼 주석으로 단계를 표시하고
  주요 인수마다 역할을 주석으로 적음
- 2단 배치 슬라이드의 코드는 40자 안쪽에서 줄바꿈

그림
- R 코드로 생성하는 그래프나 그림 안의 모든 텍스트는 영문. title은 넣고 subtitle은 넣지 않음
- setup 청크의 색상 팔레트를 재사용

내용
- 실습 안내 슬라이드는 `{background-color="#fdf0e6" .practical}` 로,
  실습 파일명과 Part 번호를 명시
- 앞선 강의 방법과의 관계를 보여주는 슬라이드 한 장
- 결과 해석 시 주의점 한 장
```

---

## 3. 실습자료

```
[공통]

첨부한 [파일명]과 같은 형식으로 「[주제명]」 실습자료를 만들어 주세요.
제목은 "[N]일차 실습 #[k]. [주제명]", Part는 [개수]개,
각 Part 예상 소요 시간은 [10분 / 20분 / ...]입니다.

구조
- 도입: 목표 한 문단 + Part 목록과 소요 시간 + 앞선 실습과 같은 점·다른 점
- `# 데이터 준비`: 실행만 하면 되는 완성 코드. 이번에 추가된 부분만 설명.
  비교 기준이 될 영모형 성능을 여기서 계산
- `# Part N` → `## Task N`, 마지막 Part는 `(선택사항)`
- `# 정리`: 확인한 것 + 응용 과제 3개
- 각 Task 끝에 `<br>`

Task는 네 부분으로 (가장 중요)
1. 지시문 — 무엇을 만들어 어디에 저장하는지까지 명시.
   예: "`rpart()`에 `form`과 `data = train`을 넣어 결과를 `tree_basic`에 저장합니다"
   함수의 각 인수가 무엇을 뜻하는지 불릿으로 나열
2. 빈 청크 — `# 아래의 결과를 복제하는 코드 작성`
3. 해답 청크 — `#| echo: false`
4. 결과 읽는 법 — 출력의 어느 숫자를 봐야 하고 그 값이 무엇을 뜻하는지

평가 절차 (자료 간 동일하게 유지)
- 연속형: `predict()` → RMSE, 영모형 대비 개선율 함께
- 이진형: 예측확률 → 기준값 0.50 → 혼동 행렬 → ROC/AUC
- 하이퍼파라미터는 훈련 데이터 안에서 교차검증, 테스트 데이터는 마지막에 한 번만
- 끝에 모든 모형을 한 표로 비교

그 밖에
- 오래 걸리는 청크에는 `callout-warning`으로 예상 시간과 축소 방법
- 패키지는 `if (!requireNamespace(...)) install.packages(...)`
- 보조 설명은 `callout-note collapse="true"`
```

---

## 만든 뒤 확인할 것

- 변수명이 실제 데이터와 일치하는지 (OHIE는 wave별 접미사가 다름)
- 처치변수의 0/1 코딩 방향
- 패키지 버전에 따른 인수 이름 변경 (`xgb.plot.tree`, `keras3` 등)
- 슬라이드 분량: 1분에 1장 기준
- 실습 분량: Task 하나당 10분 기준
